<form method="post" action="{{ path('hunter_delete', {'id': hunter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hunter.id) }}">
    <button class="btn">Delete</button>
</form>
