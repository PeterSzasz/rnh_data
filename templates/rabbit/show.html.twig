{% extends 'base.html.twig' %}

{% block title %}Rabbit{% endblock %}

{% block body %}
    <h1>Rabbit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rabbit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rabbit.name }}</td>
            </tr>
            <tr>
                <th>Speed</th>
                <td>{{ rabbit.speed }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ rabbit.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rabbit_index') }}">back to list</a>

    <a href="{{ path('rabbit_edit', {'id': rabbit.id}) }}">edit</a>

    {{ include('rabbit/_delete_form.html.twig') }}
{% endblock %}
